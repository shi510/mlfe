file(GLOB_RECURSE util_srcs "*.cc")
file(GLOB_RECURSE util_hdrs "*.h")
if(MLFE_LITE)
    foreach(FILE ${util_srcs})
        if ("${FILE}" MATCHES ".*tensorboard.*")
            list(REMOVE_ITEM util_srcs ${FILE})
        endif()
    endforeach()
    foreach(FILE ${util_hdrs})
        if ("${FILE}" MATCHES ".*tensorboard.*")
            list(REMOVE_ITEM util_hdrs ${FILE})
        endif()
    endforeach()
endif()
#
# It is temporally disabled for v2.
#
foreach(FILE ${util_srcs})
    if ("${FILE}" MATCHES ".*onnx.*")
        list(REMOVE_ITEM util_srcs ${FILE})
    endif()
endforeach()
foreach(FILE ${util_hdrs})
    if ("${FILE}" MATCHES ".*onnx.*")
        list(REMOVE_ITEM util_hdrs ${FILE})
    endif()
endforeach()
list(APPEND mlfe_source_files ${util_srcs})
list(APPEND mlfe_header_files ${util_hdrs})
set(mlfe_source_files ${mlfe_source_files} PARENT_SCOPE)
set(mlfe_header_files ${mlfe_header_files} PARENT_SCOPE)
